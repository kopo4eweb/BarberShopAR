<h2>Записаться</h2>
<p>На этой странице можете записаться к нам в Barber Shop</p>

<form action="/visit" method="POST">
    <div class="form-group">
        <label>Ваше имя</label>
        <input type="text" class="form-control" name="client[name]" placeholder="Введите Ваше имя" value="<%= @c.name %>">
    </div>

    <div class="form-group">
            <label>Ваш телефон</label>
            <input type="text" class="form-control" name="client[phone]" placeholder="Введите Ваш телефон" value="<%= @c.phone %>">
    </div>

    <div class="form-group">
        <label>Когда вас ждать?</label>
        <input type="text" class="form-control datetimepicker" name="client[datestamp]" placeholder="Введите дату и время" value="<%= @c.datestamp %>">
    </div>

    <div class="form-group">
        <label>Выбирите мастера</label>
        <select class="form-control" name="client[barber]">
            <option value="Any">Any</option>
            <% @barbers.each do |barber| %>
                <option value="<%= barber.name %>" <%= @c.barber == barber.name ? 'selected' : '' %>><%= barber.name %></option>            
            <% end %>
        </select>
    </div>

    <div class="form-group">
        <label>В какой цвет хотите себя окрасить?</label>
        <input type="color" class="form-control" name="client[color]" value="<%= @c.color %>">
    </div>

    <button type="submit" class="btn btn-primary">Записаться</button>
</form>